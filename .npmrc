# SINA monorepo — pnpm settings
# Keep the dependency graph honest: packages must declare what they import.
auto-install-peers=true
strict-peer-dependencies=false
# Link workspace packages by default so internal deps resolve to local source.
link-workspace-packages=true
prefer-workspace-packages=true
# Hoist nothing implicitly that workspaces don't declare.
shamefully-hoist=false
