/**
 * External destinations shared by the two chromes (marketing nav + docs header).
 * One constant so the source link can never drift between them.
 */
export const GITHUB_URL = "https://github.com/stritar/sina";
