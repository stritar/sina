---
title: DonutChart
---

import { DonutChartHero, DonutChartExamples } from "@/app/components/docs/demos/primitives/donut-chart";

<DonutChartHero />

`DonutChart` is a [PieChart](/docs/primitives/pie-chart) with a cutout, so you can
place a total or label in the middle.

## Usage

```tsx
import { DonutChart } from "@sina-design-system/core";

<DonutChart label="Holdings" data={{ labels, datasets }} centerLabel="$48k" />
```

## Examples

<DonutChartExamples />

## Props

<PropsTable
  rows={[
    { prop: "label", type: "string", description: "Required accessible name." },
    { prop: "data", type: "ChartData", description: "The parts to plot (Chart.js shape)." },
    { prop: "centerLabel", type: "ReactNode", description: "Content shown in the cutout." },
  ]}
/>

## Accessibility

Shares the color-blind-safe palette and the `role="img"` + text-legend accessibility
of PieChart.
