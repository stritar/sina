---
title: Checkbox
sourceHash: 5ad4cc09f477
---

import { CheckboxHero, CheckboxExamples } from "@/app/components/docs/demos/primitives/checkbox";
import { checkboxPropRows } from "@/app/components/docs/props/checkbox.props.mjs";

<CheckboxHero />

`Checkbox` 切换单个布尔值。它还支持**不确定**状态，用于表示"部分选中而非全选"，就像一个父级控制一组子项。

## 用法

```tsx
import { Checkbox } from "@sina-design-system/core";

<Checkbox checked={agreed} onCheckedChange={setAgreed} label="I agree" />
<Checkbox checked="indeterminate" label="Select all" />
```

## 示例

<CheckboxExamples />

## Props

<PropsTable rows={checkboxPropRows("zh")} />

## 无障碍

`Space` 键切换；标签可点击；状态会被朗读为已选中、未选中或混合。
