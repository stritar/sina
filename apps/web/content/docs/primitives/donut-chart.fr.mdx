---
title: DonutChart
sourceHash: d4691f5cb4d0
---

import { DonutChartHero, DonutChartExamples } from "@/app/components/docs/demos/primitives/donut-chart";
import { donutChartPropRows } from "@/app/components/docs/props/donut-chart.props.mjs";

<DonutChartHero />

`DonutChart` est un [PieChart](/docs/primitives/pie-chart) avec un évidement
central, ce qui vous permet de placer un total ou un libellé au milieu.

## Utilisation

```tsx
import { DonutChart } from "@sina-design-system/core";

<DonutChart label="Holdings" data={{ labels, datasets }} centerLabel="$48k" />
```

## Exemples

<DonutChartExamples />

## Props

<PropsTable rows={donutChartPropRows("fr")} />

## Accessibilité

Partage la palette adaptée au daltonisme ainsi que l'accessibilité de PieChart
via `role="img"` + une légende textuelle.
