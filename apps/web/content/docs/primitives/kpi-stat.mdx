---
title: KpiStat
description: A headline number with an optional trend — the stat tile for dashboards and summaries.
---

import { KpiStatHero, KpiStatExamples } from "@/app/components/docs/demos/primitives/kpi-stat";

<KpiStatHero />

`KpiStat` shows one important number, big, with an optional comparison that
renders as a trend badge (up is good by default).

## Usage

```tsx
import { KpiStat } from "@sina-design-system/core";

<KpiStat
  value={12500}
  comparisonValue={11800}
  valueFormatter={(v) => `$${v.toLocaleString("en-US")}`}
  comparisonLabel="vs last month"
/>
```

## Examples

<KpiStatExamples />

## Props

<PropsTable
  rows={[
    { prop: "value", type: "number | null", description: "The headline figure; null renders displayNullAs." },
    { prop: "valueFormatter", type: "(value: number) => string", description: "Formats the value and the delta (currency etc.)." },
    { prop: "comparisonValue", type: "number", description: "A prior value — renders the trend pill." },
    { prop: "comparisonLabel", type: "string", description: "Context under the pill, e.g. “vs previous period”." },
    { prop: "showChangeAsPercentage", type: "boolean", default: "false", description: "Show the delta as a percentage." },
    { prop: "size", type: '"sm" | "md" | "lg"', default: '"md"', description: "Tile scale." },
    { prop: "invertChangeColors", type: "boolean", default: "false", description: "When down is good (e.g. spending)." },
  ]}
/>

## Accessibility

A falling trend uses a danger badge with a bold down-glyph — meaning never rests on
color alone. Everything is real text (no image, no canvas), and it's
server-mountable (no client JS).
