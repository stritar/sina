---
title: BarChart
sourceHash: 3bdac680cbce
---

import { BarChartHero, BarChartExamples } from "@/app/components/docs/demos/primitives/bar-chart";
import { barChartPropRows } from "@/app/components/docs/props/bar-chart.props.mjs";

<BarChartHero />

`BarChart` compare des valeurs entre catégories. Activez `horizontal` pour les
libellés longs et `stacked` pour montrer la composition.

## Utilisation

```tsx
import { BarChart } from "@sina-design-system/core";

<BarChart
  label="Spending by category"
  data={{ labels, datasets }}
  horizontal
  valueFormatter={(n) => `$${n}`}
/>
```

## Exemples

<BarChartExamples />

## Props

<PropsTable rows={barChartPropRows("fr")} />

## Accessibilité

Comme tous les graphiques canvas, c'est `"use client"`, `role="img"`, et il
s'accompagne d'un résumé textuel comme voie accessible.
