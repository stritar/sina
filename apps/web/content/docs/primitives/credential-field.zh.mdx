---
title: CredentialField
sourceHash: 9c37ee883207
---

import { CredentialFieldHero, CredentialFieldExamples } from "@/app/components/docs/demos/primitives/credential-field";

<CredentialFieldHero />

`CredentialField` 在输入时对密文进行遮罩。它的配套组件 `CredentialOTP`
渲染一个分段的一次性验证码输入框，即 SINA 受治理的审批流程中的第二重因素。

## 用法

```tsx
import { CredentialField, CredentialOTP } from "@sina-design-system/core";

<CredentialField label="API key" />
<CredentialOTP length={6} onComplete={verify} />
```

## 示例

<CredentialFieldExamples />

## Props

<PropsTable
  rows={[
    { prop: "length", type: "number", default: "6", description: "CredentialOTP：位数。" },
    { prop: "onComplete", type: "(code: string) => void", description: "CredentialOTP：当每一位数字都输入完成时触发。" },
    { prop: "label / description / error", type: "ReactNode", description: "字段接线，一如往常。" },
  ]}
/>

该值绝不会以明文回显，并且与所有敏感字段一样，它会通过脱敏从审计日志中被移除。
