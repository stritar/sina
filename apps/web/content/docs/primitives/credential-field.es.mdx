---
title: CredentialField
sourceHash: 9c37ee883207
---

import { CredentialFieldHero, CredentialFieldExamples } from "@/app/components/docs/demos/primitives/credential-field";

<CredentialFieldHero />

`CredentialField` enmascara un secreto a medida que se escribe. Su componente
asociado `CredentialOTP` muestra un campo segmentado para códigos de un solo
uso, el segundo factor en los flujos de aprobación gobernados de SINA.

## Uso

```tsx
import { CredentialField, CredentialOTP } from "@sina-design-system/core";

<CredentialField label="API key" />
<CredentialOTP length={6} onComplete={verify} />
```

## Ejemplos

<CredentialFieldExamples />

## Props

<PropsTable
  rows={[
    { prop: "length", type: "number", default: "6", description: "CredentialOTP: el número de dígitos." },
    { prop: "onComplete", type: "(code: string) => void", description: "CredentialOTP: se dispara cuando se han introducido todos los dígitos." },
    { prop: "label / description / error", type: "ReactNode", description: "Conexión del campo, como de costumbre." },
  ]}
/>

El valor nunca se muestra en texto plano y, como todos los campos sensibles, se
elimina del registro de auditoría mediante redacción.
