---
title: CredentialField
sourceHash: 9c37ee883207
---

import { CredentialFieldHero, CredentialFieldExamples } from "@/app/components/docs/demos/primitives/credential-field";

<CredentialFieldHero />

`CredentialField` maskiert ein Geheimnis während der Eingabe. Seine
Begleitkomponente `CredentialOTP` rendert ein segmentiertes Eingabefeld für
Einmalcodes, den zweiten Faktor in SINAs geregelten Freigabeabläufen.

## Verwendung

```tsx
import { CredentialField, CredentialOTP } from "@sina-design-system/core";

<CredentialField label="API key" />
<CredentialOTP length={6} onComplete={verify} />
```

## Beispiele

<CredentialFieldExamples />

## Props

<PropsTable
  rows={[
    { prop: "length", type: "number", default: "6", description: "CredentialOTP: die Anzahl der Ziffern." },
    { prop: "onComplete", type: "(code: string) => void", description: "CredentialOTP: wird ausgelöst, sobald jede Ziffer eingegeben ist." },
    { prop: "label / description / error", type: "ReactNode", description: "Feldverdrahtung, wie üblich." },
  ]}
/>

Der Wert wird niemals im Klartext ausgegeben und wird, wie alle sensiblen
Felder, durch Redaktion aus dem Audit-Log entfernt.
