---
title: CredentialField
sourceHash: 9c37ee883207
---

import { CredentialFieldHero, CredentialFieldExamples } from "@/app/components/docs/demos/primitives/credential-field";

<CredentialFieldHero />

`CredentialField` は入力に合わせて秘密情報をマスクします。その対となる
`CredentialOTP` は分割された使い捨てコードの入力欄をレンダリングします。これは
SINA の統制された承認フローにおける第二要素です。

## 使い方

```tsx
import { CredentialField, CredentialOTP } from "@sina-design-system/core";

<CredentialField label="API key" />
<CredentialOTP length={6} onComplete={verify} />
```

## 例

<CredentialFieldExamples />

## Props

<PropsTable
  rows={[
    { prop: "length", type: "number", default: "6", description: "CredentialOTP: 桁数。" },
    { prop: "onComplete", type: "(code: string) => void", description: "CredentialOTP: すべての桁が入力されたときに発火します。" },
    { prop: "label / description / error", type: "ReactNode", description: "フィールドの配線。いつもどおりです。" },
  ]}
/>

値が平文で表示されることは決してなく、すべての機微なフィールドと同様に、
リダクションによって監査ログから除外されます。
