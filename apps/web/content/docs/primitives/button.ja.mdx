---
title: Button
sourceHash: 7de5af4b2e5a
---

import { ButtonHero, ButtonExamples } from "@/app/components/docs/demos/primitives/button";

<ButtonHero />

`Button` はアクションを起動します。フォームの送信、ダイアログのオープン、ステップの確定などです。
アクションには Button を、ナビゲーションにはリンクを使用します。

## 使い方

```tsx
import { Button } from "@sina-design-system/core";

<Button variant="primary" onClick={submit}>Continue</Button>
```

## 例

<ButtonExamples />

## Props

<PropsTable
  rows={[
    { prop: "variant", type: '"primary" | "secondary" | "danger" | "ghost"', default: '"primary"', description: "視覚的な重みとインテント。" },
    { prop: "size", type: '"sm" | "md" | "lg" | "xl"', default: '"md"', description: "コントロールの高さ。" },
    { prop: "iconLeft / iconRight", type: "ReactNode", description: "先頭または末尾のグリフ。" },
    { prop: "loading", type: "boolean", default: "false", description: "スピナーを表示し、ボタンを無効にします。" },
    { prop: "asChild", type: "boolean", default: "false", description: "ボタンのスタイルを保ちつつ、子要素（例：リンク）としてレンダリングします。" },
  ]}
/>

## アクセシビリティ

ネイティブの `<button>` セマンティクス。フォーカス可能で、`Enter`/`Space` で起動し、
`disabled` が読み上げられます。アイコンのみのボタンには `aria-label` が必要です。
